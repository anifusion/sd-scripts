# =============================================================================
# Optional / library-only (see sd-scripts/library)
# =============================================================================
# Intel Extension for PyTorch: attention slicing (defaults in code apply if unset)
IPEX_FORCE_ATTENTION_SLICE=
IPEX_ATTENTION_SLICE_RATE=4
IPEX_SDPA_SLICE_TRIGGER_RATE=4

# DeepSpeed distributed training (`library/deepspeed_utils.py` expects this when enabled)
WORLD_SIZE=
